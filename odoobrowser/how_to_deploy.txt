# For theia
vsce package
mv *.vsix releases
rm latest.vsix
ln -s $(ls . |tail -n1) latest.vsix
git tag -f VERSION
git push --tags

# to MS marketplace
increase version in package.json
vsce package
vsce publish

# just create the vsix file
increase version in package.json
vsce package

Installation in code server:
code --install-extension <path to file>